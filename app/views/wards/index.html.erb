<h1>Wards</h1>

<table class="table table-bordered table-striped">
  <tr>
    <th>Wards</th>
    <th></th>
  </tr>

  <% @wards.each do |ward| %>
    <tr>
        <td><%= ward.name %></td>
        <td><%= link_to 'Show', ward_path(ward) %></td>
    </tr>
  <% end %>
</table>