<h1>Wards</h1>

<table>
  <tr>
    <th>Wards</th>
    <th></th>
  </tr>

  <% @wards.each do |ward| %>
    <tr>
        <td><%= ward.ward %></td>
        <td><%= link_to 'Show', ward_path(ward) %></td>
    </tr>
  <% end %>
</table>